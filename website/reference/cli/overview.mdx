---
title: CLI
description: Fission project and target management commands.
---

# CLI overview

Primary commands:

- `fission init <app>`
- `fission add-target <target>`
- `fission run <target>`
- `fission build <target>`

Use these to create projects consistently and keep target metadata in local project descriptors.
