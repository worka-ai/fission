---
title: Slider
description: Reference details for Slider.
slug: /widgets/slider
---

# Slider

An input-focused control for converting user intent into typed actions.


## What this is for
`Slider` is part of Fission's widget API and fits the deterministic, state-driven render model.

## Why you use it
- Keep UI behavior deterministic by composing from explicit nodes.
- Use it where behavior needs to stay testable and reviewable.
- Connect it through your existing action/reducer and environment flow.

## Minimal usage
```rust
use fission_core::ui::Slider;

let node = Slider {
    ..Default::default()
}.into_node();
```

## Config surface
- `id`: `Option<NodeId>`
- `value`: `f32`
- `min`: `f32`
- `max`: `f32`
- `on_change`: `Option<ActionEnvelope>`

## Learn next
- [Widget catalog](/reference/widgets/catalog)
- [Core widget contract](/reference/core/widget-trait)

