---
title: TextRunStyle
description: Reference details for TextRunStyle.
slug: /widgets/text-run-style
---

# TextRunStyle

A public widget/API entrypoint in the Fission surface.


## What this is for
`TextRunStyle` is part of Fission's widget API and fits the deterministic, state-driven render model.

## Why you use it
- Keep UI behavior deterministic by composing from explicit nodes.
- Use it where behavior needs to stay testable and reviewable.
- Connect it through your existing action/reducer and environment flow.

## Minimal usage
```rust
use fission_core::ui::TextRunStyle;

let node = TextRunStyle {
    ..Default::default()
}.into_node();
```

## Config surface
- `font_size`: `Option<f32>`
- `color`: `Option<IrColor>`
- `underline`: `bool`
- `font_family`: `Option<String>`
- `locale`: `Option<String>`
- `font_weight`: `Option<u16>`
- `font_style`: `TextFontStyle`
- `line_height`: `Option<f32>`
- `letter_spacing`: `Option<f32>`
- `text_scale`: `Option<f32>`
- `background_color`: `Option<IrColor>`

## Learn next
- [Widget catalog](/reference/widgets/catalog)
- [Core widget contract](/reference/core/widget-trait)

