---
title: Switch
description: Reference details for Switch.
slug: /widgets/switch
---

# Switch

An input-focused control for converting user intent into typed actions.


## What this is for
`Switch` is part of Fission's widget API and fits the deterministic, state-driven render model.

## Why you use it
- Keep UI behavior deterministic by composing from explicit nodes.
- Use it where behavior needs to stay testable and reviewable.
- Connect it through your existing action/reducer and environment flow.

## Minimal usage
```rust
use fission_core::ui::Switch;

let node = Switch {
    ..Default::default()
}.into_node();
```

## Config surface
- `id`: `Option<NodeId>`
- `checked`: `bool`
- `on_toggle`: `Option<ActionEnvelope>`

## Learn next
- [Widget catalog](/reference/widgets/catalog)
- [Core widget contract](/reference/core/widget-trait)

