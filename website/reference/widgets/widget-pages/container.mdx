---
title: Container
description: Reference details for Container.
slug: /widgets/container
---

# Container

A layout and composition primitive for predictable structure and spacing.


## What this is for
`Container` is part of Fission's widget API and fits the deterministic, state-driven render model.

## Why you use it
- Keep UI behavior deterministic by composing from explicit nodes.
- Use it where behavior needs to stay testable and reviewable.
- Connect it through your existing action/reducer and environment flow.

## Minimal usage
```rust
use fission_core::ui::Container;

let node = Container {
    ..Default::default()
}.into_node();
```

## Config surface
- `id`: `Option<NodeId>`
- `child`: `Option<Box<Node>>`
- `width`: `Option<f32>`
- `height`: `Option<f32>`
- `min_width`: `Option<f32>`
- `max_width`: `Option<f32>`
- `min_height`: `Option<f32>`
- `max_height`: `Option<f32>`
- `padding`: `[f32; 4]`
- `flex_grow`: `f32`
- `flex_shrink`: `f32`
- `background_fill`: `Option<Fill>`

## Learn next
- [Widget catalog](/reference/widgets/catalog)
- [Core widget contract](/reference/core/widget-trait)

