---
title: GestureDetector
description: Reference details for GestureDetector.
slug: /widgets/gesture-detector
---

# GestureDetector

A layout and composition primitive for predictable structure and spacing.


## What this is for
`GestureDetector` is part of Fission's widget API and fits the deterministic, state-driven render model.

## Why you use it
- Keep UI behavior deterministic by composing from explicit nodes.
- Use it where behavior needs to stay testable and reviewable.
- Connect it through your existing action/reducer and environment flow.

## Minimal usage
```rust
use fission_core::ui::GestureDetector;

let node = GestureDetector {
    ..Default::default()
}.into_node();
```

## Config surface
- `id`: `Option<NodeId>`
- `child`: `Box<Node>`
- `on_tap`: `Option<ActionEnvelope>`
- `on_double_tap`: `Option<ActionEnvelope>`
- `on_long_press`: `Option<ActionEnvelope>`
- `on_drag_start`: `Option<ActionEnvelope>`
- `on_drag_update`: `Option<ActionEnvelope>`
- `on_drag_end`: `Option<ActionEnvelope>`
- `on_hover_enter`: `Option<ActionEnvelope>`
- `on_hover_exit`: `Option<ActionEnvelope>`
- `on_drop`: `Option<ActionEnvelope>`
- `on_secondary_click`: `Option<ActionEnvelope>`

## Learn next
- [Widget catalog](/reference/widgets/catalog)
- [Core widget contract](/reference/core/widget-trait)

