---
title: Tabs
description: Reference details for Tabs.
slug: /widgets/tabs
---

# Tabs

A navigation utility used to shape app-level flow and compositional patterns.


## What this is for
`Tabs` is part of Fission's widget API and fits the deterministic, state-driven render model.

## Why you use it
- Keep UI behavior deterministic by composing from explicit nodes.
- Use it where behavior needs to stay testable and reviewable.
- Connect it through your existing action/reducer and environment flow.

## Minimal usage
```rust
use fission_widgets::Tabs;

let node = Tabs {
    ..Default::default()
}.into_node();
```

## Config surface
- `active_index`: `usize`
- `items`: `Vec<TabItem>`

## Learn next
- [Widget catalog](/reference/widgets/catalog)
- [Core widget contract](/reference/core/widget-trait)

