---
title: TerminalView
description: Reference details for TerminalView.
slug: /widgets/terminal-view
---

# TerminalView

A terminal integration primitive for desktop command-driven workflows.


## What this is for
`TerminalView` is part of Fission's widget API and fits the deterministic, state-driven render model.

## Why you use it
- Keep UI behavior deterministic by composing from explicit nodes.
- Use it where behavior needs to stay testable and reviewable.
- Connect it through your existing action/reducer and environment flow.

## Minimal usage
```rust
use fission_widgets::TerminalView;

let node = TerminalView {
    ..Default::default()
}.into_node();
```

## Config surface
- `session`: `Arc<TerminalSession>`
- `viewport_width`: `f32`
- `viewport_height`: `f32`
- `font_size`: `f32`
- `line_height`: `f32`
- `padding_x`: `f32`
- `padding_y`: `f32`

## Learn next
- [Widget catalog](/reference/widgets/catalog)
- [Core widget contract](/reference/core/widget-trait)

