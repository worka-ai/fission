---
title: RangeSlider
description: Reference details for RangeSlider.
slug: /widgets/range-slider
---

# RangeSlider

An input-focused control for converting user intent into typed actions.


## What this is for
`RangeSlider` is part of Fission's widget API and fits the deterministic, state-driven render model.

## Why you use it
- Keep UI behavior deterministic by composing from explicit nodes.
- Use it where behavior needs to stay testable and reviewable.
- Connect it through your existing action/reducer and environment flow.

## Minimal usage
```rust
use fission_widgets::RangeSlider;

let node = RangeSlider {
    ..Default::default()
}.into_node();
```

## Config surface
- `id`: `Option<NodeId>`
- `start`: `f32`
- `end`: `f32`
- `min`: `f32`
- `max`: `f32`
- `on_change`: `Option<ActionEnvelope>`

## Learn next
- [Widget catalog](/reference/widgets/catalog)
- [Core widget contract](/reference/core/widget-trait)

