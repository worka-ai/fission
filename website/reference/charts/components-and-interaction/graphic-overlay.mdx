---
title: Graphic overlay
sidebar_label: Graphic overlay
description: Fission Charts reference for Graphic overlay.
---

# Graphic overlay

![Graphic overlay screenshot](/img/charts/graphic-overlay.png)

Typed graphic shapes and text layered over the chart plot area. Use it for callouts, annotations, labels, and lightweight explanatory overlays. The screenshot is captured from the native Fission chart gallery.

## What the chart is for

Graphic overlay belongs to the **Components and interaction** family. Its job is to make this data shape readable: `ChartGraphic rect, text, line, or circle records.` In a production interface, choose this chart when the visual form makes the user's question faster to answer than a table or a simpler chart would.

Avoid it when the visual form makes the user estimate more than necessary; choose the simplest chart that answers the product question.

## Data model

`ChartGraphic rect, text, line, or circle records.` Keep the data close to the type that describes it. Fission Charts is typed Rust, so a line uses `LineSeries`, a calendar heatmap uses `CalendarHeatmapSeries`, and chart components such as zoom, marks, and graphics are explicit fields on `Chart` rather than hidden string configuration.

## Rust API

| Field | Type | Notes |
| --- | --- | --- |
| `title` | `&str` | Names the chart for the screen, accessibility tree, and test output. |
| `graphic` | `ChartGraphic` | Adds typed rect, circle, text, or line overlays. |
| `width / height` | `f32` | Optional fixed size; omit them when the chart should flex inside Fission layout. |

## Example

```rust
use fission_charts::{Axis, Chart, ChartGraphic, LineSeries};
use fission_core::op::Color;

let chart = Chart::new()
    .title("Graphic overlay")
    .x_axis(Axis::category(vec!["Mon", "Tue", "Wed", "Thu", "Fri"]))
    .y_axis(Axis::value())
    .graphic(ChartGraphic::text(0.20, 0.12, "release window", Color { r: 37, g: 99, b: 235, a: 255 }))
    .series(vec![LineSeries::new("Latency").data(vec![80.0, 132.0, 101.0, 184.0, 140.0]).into()]);
```

## Interaction and animation

Charts can emit typed `ChartInteractionEvent` values when interaction is enabled. Handle those events in a reducer when the app needs hover, press, release, scroll, selection, or brush behavior. `ChartAnimation` stores duration, delay, stagger, easing, and reduced-motion behavior as deterministic chart data, so animation timing can be tested instead of being hidden in ad-hoc timers.

## Testing guidance

For this chart, test the data mapping first, then test lowering, then capture a live screenshot when visual output changes. The screenshot for this page is refreshed with `npm run charts:generate` from the website package.

Tags: `graphic`, `annotation`.


[Components and interaction family overview](./overview)
