[package]
name = "fission-render-vello"
version = "0.1.0"
edition = "2021"

[dependencies]
fission-render = { path = "../fission-render" }
fission-layout = { path = "../../core/fission-layout" }
anyhow = "1.0"
vello = { version = "0.3.0", features = ["wgpu"] } # Ensure wgpu is enabled if needed, though default is usually yes
# We'll stick to simple 0.3.0 for now