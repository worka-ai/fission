# fission-shell-server

Server-side web shell for Fission routes. It renders normal Fission widget trees to HTML, caches revalidated pages behind a cache trait, and records route-local worker and WASM island metadata.
