[package]
name = "fission-shell"
version = "0.1.0"
edition = "2021"
license = "MIT"
repository = "https://github.com/worka-ai/fission"
description = "Shell"

[dependencies]
fission-core = { path = "../../core/fission-core", version = "0.1.0" }
fission-ir = { path = "../../core/fission-ir", version = "0.1.0" }
fission-render = { path = "../../rendering/fission-render", version = "0.1.0" } # Corrected path
serde = { version = "1.0", features = ["derive"] }
anyhow = "1.0"

[dev-dependencies]
serde_json = "1.0"